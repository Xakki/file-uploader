name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      core: ${{ steps.f.outputs.core }}
      js: ${{ steps.f.outputs.js }}
      php: ${{ steps.f.outputs.php }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: f
        with:
          filters: |
            core:
              - 'src/**'
              - 'tests/**'
              - 'composer.json'
              - 'phpunit.xml.dist'
              - 'phpstan.neon.dist'
              - 'protocol/fixtures/**'
              - '.github/workflows/ci.yml'
            js:
              - 'js/**'
              - 'protocol/fixtures/**'
              - '.github/workflows/ci.yml'
            php:
              - 'src/**'
              - 'tests/**'
              - 'pint.json'
              - '.github/workflows/ci.yml'

  php-core:
    needs: changes
    if: ${{ needs.changes.outputs.core == 'true' }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php: ['8.3', '8.4', '8.5']
    name: php-core (PHP ${{ matrix.php }})
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: mbstring, fileinfo
          coverage: pcov
          tools: composer:v2
      - run: composer install --prefer-dist --no-progress --no-interaction
      - run: composer phpunit
      - run: composer phpstan
      - name: PHP coverage (8.4 only)
        if: matrix.php == '8.4'
        run: composer phpunit-coverage
      - name: Enforce 70% PHP coverage floor
        if: matrix.php == '8.4'
        run: |
          # the project-summary <metrics> line is the only one carrying files="
          metrics=$(grep -o '<metrics [^>]*files="[^>]*>' coverage.xml | tail -1)
          # boundary before "statements" so it doesn't also match "coveredstatements"
          total=$(echo "$metrics" | grep -oP '(?<![a-z])statements="\K[0-9]+')
          covered=$(echo "$metrics" | grep -oP 'coveredstatements="\K[0-9]+')
          pct=$(awk "BEGIN { if ($total>0) printf \"%.0f\", 100*$covered/$total; else print 0 }")
          echo "PHP coverage: ${pct}% (${covered}/${total} statements)"
          if [ "$pct" -lt 70 ]; then echo "FAIL: below 70% floor"; exit 1; fi

  pint:
    needs: changes
    if: ${{ needs.changes.outputs.php == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          coverage: none
          tools: composer:v2
      - run: composer global require laravel/pint --no-progress
      - run: "$(composer global config home)/vendor/bin/pint --test src tests"

  js:
    needs: changes
    if: ${{ needs.changes.outputs.js == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: js/package-lock.json
      - run: npm ci
        working-directory: js
      - run: npm run typecheck
        working-directory: js
      - run: npm run test:coverage
        working-directory: js
      - run: npm run build
        working-directory: js

  ci:
    if: always()
    needs: [php-core, pint, js]
    runs-on: ubuntu-latest
    steps:
      - name: Verify all jobs succeeded or were skipped
        run: |
          for r in \
            "${{ needs.php-core.result }}" \
            "${{ needs.pint.result }}" \
            "${{ needs.js.result }}"; do
            echo "result: $r"
            if [ "$r" = "failure" ] || [ "$r" = "cancelled" ]; then
              exit 1
            fi
          done
